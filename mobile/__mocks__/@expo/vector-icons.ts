import React from "react";
export const Ionicons = (props: any) => React.createElement("View", props);
export const MaterialIcons = (props: any) => React.createElement("View", props);
export default { Ionicons, MaterialIcons };
